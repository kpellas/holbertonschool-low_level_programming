#include <stdio.h>
/**
* main - Prints numbesr of base 16
*
* Return: 0 on success
*/
int main(void)
{

	char ch = 0x00;
	
	while (ch <= 0xff){
	}
		putchar(ch + 01);
		ch++;
	putchar('\n');
	return (0);
}
