#include <stdio.h>
/**
* main - Prints numbesr of base 16
*
* Return: 0 on success
*/
int main(void)
{

	char ch = 0x00;
	
	for (ch, ch < 0xff, ch++)
	}
		putchar(ch + 01);
		ch++;
	putchar('\n');
	return (0);
}
